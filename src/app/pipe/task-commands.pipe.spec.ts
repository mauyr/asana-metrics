import { TaskCommandsPipe } from './task-commands.pipe';

describe('TaskCommandsPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskCommandsPipe();
    expect(pipe).toBeTruthy();
  });
});

import { TaskSectionsPipe } from './task-sections.pipe';

describe('TaskSectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskSectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
